# Trino → Postgres catalog. The catalog name is the filename: "postgres".
# In the NL→SQL card use: source=trino, catalog=postgres, schema=public
connector.name=postgresql
connection-url=jdbc:postgresql://postgres:5432/security
connection-user=plexus
connection-password=plexus
