# core (required)
fastapi>=0.110
uvicorn[standard]>=0.27
pydantic>=2.6
pydantic-settings>=2.1
python-dotenv>=1.0

# connectors (only needed when PLEXUS_DEMO_MODE=false)
boto3>=1.34
neo4j>=5.18
trino>=0.328
anthropic>=0.40       # direct Anthropic API (NL2SQL agent)
mcp>=1.0
openai>=1.40
azure-identity>=1.17  # only needed for Entra ID (azure_use_entra)
